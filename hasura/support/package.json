{
  "private": true,
  "scripts": {
    "deploy": "now deploy",
    "deploy-to-prod": "now deploy --prod"
  },
  "dependencies": {
    "express": "^4.17.1",
    "pg": "^8.0.2"
  },
  "devDependencies": {
    "now": "^18.0.0"
  }
}
