{
  "private": true,
  "scripts": {
    "dev-migrate-apply": "hasura migrate apply --admin-secret admin_secret",
    "dev-console": "hasura console --admin-secret admin_secret",
    "prod-console": "hasura console --admin-secret SECRET --endpoint https://mylocalshop-hasura-engine.herokuapp.com",
    "prod-migrate-apply": "hasura migrate apply --admin-secret SECRET --endpoint https://mylocalshop-hasura-engine.herokuapp.com"
  },
  "devDependencies": {
    "hasura-cli": "^1.1.1"
  }
}
