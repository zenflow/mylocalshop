- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "public"."users"
                 add constraint "users_role_fkey"
                 foreign key ("role")
                 references "public"."user_roles"
                 ("value") on update no action on delete restrict;
  type: run_sql
